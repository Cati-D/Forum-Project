@*@model Anspeamiaincercareplusunu.Models.PostReply
@{
    ViewBag.Title = "Lasa un raspuns...";
}
<br />
<h3 class="text-center">@ViewBag.Title</h3>
<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <div class="well">
            <div class="col-sm-10 text-left">
                <div class="row"><strong>@Model.User</strong></div>
                <div class="row"><small>@Model.Created.ToShortDateString()</small></div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12">
                <p>@Model.Content</p>
            </div>
        </div>
    </div>
</div>
<br />
<div class="row">
    <div class="col-sm-4 col-sm-offset-4">
        <form method="post">
            @Html.ValidationSummary("", new { @class = "text-danger" })
            <div class="form-group">
                @Html.LabelFor(m => m.Content)
                @Html.TextBoxFor(m => m.Content, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Content, "", new { @class = "text-danger" })

            </div>

            <input name="Id" class="form-control" type="hidden" value="@Model.Id" />
            <input name="Created" class="form-control" type="hidden" value="@Model.Created" />
            <input name="Content" class="form-control" type="hidden" value="@Model.Content" />
            <input name="User" class="form-control" type="hidden" value="@Model.User" />
            <button type="submit" class="btn btn-primary">Submit</button>
            <a href='javascript:history.go(-1)' class="btn btn-default">Cancel</a>
        </form>
    </div>
</div>
    *@